import { FormData } from '../types/form.types';

/**
 * Enviar dados de inscrição para o servidor.
 * TODO: Implemente a URL e lógica do backend aqui.
 */
export async function submitInscricao(_data: FormData): Promise<void> {
  // Exemplo de estrutura:
  // const body = new FormData();
  // body.append('nomeCompleto', data.nomeCompleto);
  // ...
  // await fetch('https://SEU_BACKEND/api/inscricao', { method: 'POST', body });
  return Promise.resolve();
}
